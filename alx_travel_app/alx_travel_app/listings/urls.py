# alx_travel_app/listings/urls.py

from django.urls import path

urlpatterns = [
    # API endpoints for your listings app will be defined here later
    # Example: path('my-first-listing-api/', views.MyListingView.as_view(), name='my-listing-api'),
]
